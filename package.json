{
  "name": "diara",
  "version": "1.0.0",
  "description": "Banish your abandoned GitHub repositories - Extra damage to all undead repos",
  "main": "src/index.js",
  "type": "module",
  "bin": {
    "diara": "./bin/diara.js"
  },
  "scripts": {
    "start": "bun src/index.js",
    "build": "bun build src/index.js --compile --outfile dist/diara",
    "build:macos-arm": "bun build src/index.js --compile --target=bun-darwin-arm64 --outfile dist/diara-macos-arm64",
    "build:macos-x64": "bun build src/index.js --compile --target=bun-darwin-x64 --outfile dist/diara-macos-x64",
    "build:linux-x64": "bun build src/index.js --compile --target=bun-linux-x64 --outfile dist/diara-linux-x64",
    "build:linux-arm": "bun build src/index.js --compile --target=bun-linux-arm64 --outfile dist/diara-linux-arm64",
    "build:all": "bun run build:macos-arm && bun run build:macos-x64 && bun run build:linux-x64 && bun run build:linux-arm"
  },
  "keywords": [
    "github",
    "cli",
    "repository",
    "delete",
    "cleanup",
    "bulk",
    "bun"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "@octokit/rest": "^21.0.0",
    "chalk": "^5.3.0",
    "conf": "^13.0.0",
    "ora": "^8.0.0"
  }
}
